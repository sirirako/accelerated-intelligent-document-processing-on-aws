[project]
name = "patterns-unified-src-bda_completion_function"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "boto3"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
