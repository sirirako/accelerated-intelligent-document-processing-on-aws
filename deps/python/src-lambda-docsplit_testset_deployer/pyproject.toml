[project]
name = "src-lambda-docsplit_testset_deployer"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pypdf>=4.0.0",
    "requests>=2.28.0",
    "cfnresponse"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
