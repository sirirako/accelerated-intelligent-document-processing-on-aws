[project]
name = "src-lambda-start_codebuild"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "crhelper~=2.0.10"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
