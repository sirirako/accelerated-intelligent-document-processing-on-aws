[project]
name = "nested-bedrockkb-src-s3_vectors_manager"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "boto3>=1.40.25",
    "cfnresponse",
    "botocore>=1.40.25"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
