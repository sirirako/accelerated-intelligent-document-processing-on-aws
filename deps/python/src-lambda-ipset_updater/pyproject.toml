[project]
name = "src-lambda-ipset_updater"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "boto3>=1.24.0",
    "urllib3>=1.26.0",
    "cfnresponse"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
