[project]
name = "nested-bedrockkb-src-opensearch_index_custom_resource"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "opensearch-py==2.8.0",
    "requests==2.33.0",
    "requests-aws4auth==1.3.1",
    "aws-lambda-powertools==2.43.0",
    "cfnresponse"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
