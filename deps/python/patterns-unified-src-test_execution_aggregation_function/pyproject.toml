[project]
name = "patterns-unified-src-test_execution_aggregation_function"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "stickler-eval==0.1.5"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
