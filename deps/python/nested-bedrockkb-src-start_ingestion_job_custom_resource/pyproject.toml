[project]
name = "nested-bedrockkb-src-start_ingestion_job_custom_resource"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "cfnresponse"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
