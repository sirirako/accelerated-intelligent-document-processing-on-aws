[project]
name = "src-lambda-w2_dataset_deployer"
version = "0.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "huggingface-hub==0.20.0",
    "pyarrow==20.0.0",
    "cfnresponse"
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
