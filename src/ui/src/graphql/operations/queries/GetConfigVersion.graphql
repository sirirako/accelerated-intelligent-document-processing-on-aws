query GetConfigVersion($versionName: String!) {
  getConfigVersion(versionName: $versionName) {
    success
    Schema
    Default
    Custom
    error {
      type
      message
      validationErrors {
        field
        message
        type
      }
    }
  }
}
