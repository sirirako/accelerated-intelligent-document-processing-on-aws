query GetChatMessages($sessionId: ID!) {
  getChatMessages(sessionId: $sessionId) {
    role
    content
    timestamp
    isProcessing
    sessionId
    messageType
    toolMetadata {
      toolName
      toolUseId
    }
  }
}
