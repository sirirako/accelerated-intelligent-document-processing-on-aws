query ListConfigurationLibrary($pattern: String!) {
  listConfigurationLibrary(pattern: $pattern) {
    success
    items {
      name
      hasReadme
      path
      configFileType
    }
    error
  }
}
