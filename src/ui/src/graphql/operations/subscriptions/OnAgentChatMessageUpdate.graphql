subscription OnAgentChatMessageUpdate($sessionId: String!) {
  onAgentChatMessageUpdate(sessionId: $sessionId) {
    role
    content
    timestamp
    isProcessing
    sessionId
    messageType
    toolMetadata {
      toolName
      toolUseId
    }
  }
}
