mutation ProbeCircuitBreaker($reason: String!) {
  probeCircuitBreaker(reason: $reason) {
    enabled
    state
    openedAt
    lastCheckedAt
    failureCount
    recoveryAttempts
    lastError
  }
}
