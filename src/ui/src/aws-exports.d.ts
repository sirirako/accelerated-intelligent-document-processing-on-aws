declare const awsExports: Record<string, unknown>;
export default awsExports;

// API transport selection + endpoints (see aws-exports.js).
export const apiBaseUrl: string;
export const streamUrl: string;
export const awsRegion: string | undefined;
