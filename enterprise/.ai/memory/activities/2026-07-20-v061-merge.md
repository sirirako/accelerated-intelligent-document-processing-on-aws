# 2026-07-20: v0.6.1 Upstream Merge

## What was done
- Synced fork's `main` from upstream main (v0.6.1) — fast-forward, no conflicts
- Merged `main` into `enterprise/develop` — 6 conflicts resolved
- Test deployed: `idp-default` (x86_64) ✅, `idp-enterprise` (arm64, private, headless) ✅

## Conflicts resolved
- `template.yaml` — trivial (PermissionsBoundary comment)
- `CLAUDE.md` — kept both skill entries
- `feature-platform/main-stack-extensions/template.yaml` — took upstream (AppSync removed)
- `patterns/unified/template.yaml` — took upstream (app code), re-added registry params
- `scripts/sdlc/cfn/codepipeline-s3.yml` — kept both (our registry + upstream's test VPC)
- `scripts/sdlc/codebuild_deployment.py` — took upstream, added load_pipeline_config

## Key findings
- v0.6.1 removed ALB hosting (only CloudFront/APIGateway now)
- v0.6.1 removed AppSync (API Gateway REST replaces it)
- v0.6.1 retired standalone assessment (moved to extraction.confidence)
- postHook schema is now upstream (not just our enterprise addition)
- In-place update from v0.5.x NOT possible (must create fresh stack)
- `idp_feature_sdk` required for publish (new dep)
- Node >= 22.12 required for UI lint (use --no-lint to skip)

## Published
- Commit: 367a68e0 (later amended multiple times for fixes)
- Pushed to origin/enterprise/develop
